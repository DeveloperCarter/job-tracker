# Backend (Planned)

This folder will host a Java 21 Spring Boot application.

Chosen package style (learning goal):
- `controller`
- `service`
- `repository`

Additional packages:
- `model`, `dto`, `config`, `exception`
