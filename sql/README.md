# SQL Workspace

You will author schema and queries manually first.

## Plan
1. Draft schema in `schema-draft.sql`.
2. Apply locally to PostgreSQL.
3. Later introduce Flyway by converting validated SQL drafts into versioned migrations.
